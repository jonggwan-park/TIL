딥러닝에서 torch(PyTorch)의 쓰임새
**torch(PyTorch)**는 딥러닝 모델을 만들고 학습하는 데 가장 많이 사용하는 프레임워크 중 하나야.
TensorFlow와 경쟁하는 딥러닝 라이브러리이며, 유연성과 직관적인 코드 덕분에 연구 및 실무에서 많이 사용돼.

1. PyTorch의 주요 기능
🔹 (1) 텐서 연산 (NumPy 대체 가능)
torch.Tensor는 numpy.array와 유사하지만, GPU 연산 지원이 가능함.
```python
import torch

# 텐서 생성
a = torch.tensor([[1, 2], [3, 4]])
b = torch.tensor([[5, 6], [7, 8]])

# 기본 연산
print(a + b)  # 덧셈
print(a * b)  # 요소별 곱
print(torch.matmul(a, b))  # 행렬 곱
```

✅ NumPy 대신 PyTorch를 쓰면 GPU 가속을 활용할 수 있음
✅ .numpy()로 변환 가능, 반대로 NumPy → PyTorch 변환도 가능

```python
import numpy as np

arr = np.array([1, 2, 3])
tensor = torch.from_numpy(arr)
print(tensor)  # tensor([1, 2, 3])

np_arr = tensor.numpy()
print(np_arr)  # array([1, 2, 3])
```

(2) GPU 가속 (CUDA 지원)
PyTorch는 딥러닝에서 GPU를 쉽게 사용할 수 있도록 설계됨.
.to("cuda")를 사용하면 GPU에서 연산 가능.

```python
device = "cuda" if torch.cuda.is_available() else "cpu"
print("Using device:", device)

x = torch.tensor([1.0, 2.0, 3.0], device=device)
y = torch.tensor([4.0, 5.0, 6.0], device=device)
print(x + y)  # GPU에서 연산 수행
```
✅ TensorFlow보다 GPU 활용이 더 직관적이고 간편함!

 (3) 자동 미분 (Autograd)
torch.autograd는 딥러닝에서 중요한 미분(Gradient 계산)을 자동으로 수행해 줌.
requires_grad=True를 설정하면, PyTorch가 자동으로 기울기(Gradient)를 추적함.

```python
x = torch.tensor(2.0, requires_grad=True)

# 수식 정의: y = x^2
y = x ** 2

# 역전파 실행 (dy/dx 계산)
y.backward()

print(x.grad)  # 2x = 2*2 = 4
```

✅ 딥러닝에서 역전파(Backpropagation)할 때 필수적인 기능!
✅ 매뉴얼로 미분을 계산할 필요 없이 자동으로 처리됨.

 (4) 신경망 구축 (torch.nn)
torch.nn은 딥러닝 모델을 쉽게 만들도록 도와주는 모듈임.
```python
import torch.nn as nn

# 간단한 신경망 정의
class SimpleNN(nn.Module):
    def __init__(self):
        super(SimpleNN, self).__init__()
        self.fc = nn.Linear(3, 1)  # 입력 3개, 출력 1개

    def forward(self, x):
        return self.fc(x)

model = SimpleNN()
print(model)
```

✅ 레이어 추가 및 모델 설계가 직관적임!
✅ PyTorch의 torch.nn을 이용하면 모델을 쉽게 정의할 수 있음.