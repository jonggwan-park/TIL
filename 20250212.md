- Retrieval
    - ‘컴퓨터에 있는 특정 데이터를 가져오는 것’을 의미한다고 생각하는 것이 이해하는데 도움이 됨
    - 즉 LLM, 언어모델이 가지고 있지 않은 정보를 ‘가져오는 것’
        
        == 언어 모델이 아웃풋을 만드는데 필요한 추가적인 정보를 제공하는 것
        
        == 언어 모델은 답변을 생성해 내는 것에 매우 능숙하지만, 그 기반이 되는 정보를 모두 가지고 있지는 않음

 Augmented (증강)
    - AR의 A와 같은 단어
    - AR?
        - Augmented Reality, 증강 현실
        - 현실 세계에 가상 세계를 덧씌워 융합하는 기술
        - 현재는 카메라나 디스플레이를 많이 이용해서 구현되어 있음

- **Retrieval Augmented Generation (RAG)**
- Retrieval == ‘검색’
    - 최초에 트레이닝 할 때 없었던 유저의 데이터를 가져와서 사용하는 방법
    - 내가 입력으로 넣는 **정보와 비슷한 것**이 특정한 곳에 있는지를 알아내는건데
        - ‘안녕?’과 ‘안녕하세요?’는 비슷한가?
        - ‘사과’와 ‘오렌지’는 비슷한가?
- 비슷한 것을 찾기위해
    
    이때 우리는 유사도(Similarity)라는걸 씁니다.

- **코사인 유사도(Cosine Similarity)**
    - 두 벡터가 이루는 각도를 계산해서 비슷한지를 보는것

- **유클리드 거리(Euclidean Distance)**
    
    → 두 벡터가 얼마나 떨어져 있는지(직선거리)를 보는것



- 컴퓨터가 알아듣도록 텍스트나 이미지를 ‘벡터(숫자)’로 바꾸는 것
    == 임베딩(Embeddings)
    
    - 하나의 숫자가 아닌 일종의 알고리즘을 통해 여러개의 숫자,
    즉 벡터(Vector) 형태로 표현하는 방법