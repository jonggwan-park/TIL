### criterion

기준, 표준, 척도

### 경사 하강법(Gradient Descent)이란?

기계 학습 모델은 목표값과 예측값의 차이를 줄이는 방향으로 학습해.
이를 위해 손실 함수(Loss Function, 오차를 측정하는 함수)의 값을 최소화하는 최적의 가중치(파라미터)를 찾는 과정이 필요해.
**경사 하강법(GD)**은 손실 함수의 기울기(경사)를 따라 내려가면서 최적의 가중치를 찾는 알고리즘이야.
기울기를 찾으려면 미분(gradient, 도함수)을 계산해서 조금씩 이동하면서 최적점을 찾음.

SGD vs. 일반적인 경사 하강법(GD)
SGD는 일반적인 경사 하강법과 다르게 데이터를 한 번에 모두 사용하지 않고, 일부만 샘플링하여 업데이트해.

✅ 1) 배치 경사 하강법 (Batch Gradient Descent, GD)
모든 데이터(전체 배치)를 사용해서 가중치를 한 번에 업데이트.
하지만 데이터가 많아지면 계산량이 너무 많아지는 단점이 있음.
```python
복사
편집
gradient = sum_of_all_gradients / total_samples
```
✅ 2) 확률적 경사 하강법 (Stochastic Gradient Descent, SGD)
전체 데이터를 사용하지 않고, 무작위로 한 개의 샘플을 골라서 업데이트.
데이터가 많아도 빠르게 학습할 수 있음!
하지만 진동(Noise)이 심할 수 있음.
```python
gradient = single_sample_gradient
```
✅ 3) 미니배치 경사 하강법 (Mini-Batch Gradient Descent)
GD와 SGD의 절충안으로, 여러 개의 샘플(미니배치)로 업데이트.
보통 미니배치 크기를 32, 64, 128 같은 값으로 설정.
```python
gradient = sum_of_mini_batch_gradients / batch_size
```

왜 SGD를 사용할까? (장점과 단점)
✅ 장점
빠른 학습 속도 🚀
→ 전체 데이터를 계산하는 Batch GD보다 훨씬 빠름
큰 데이터셋에서 유리함
→ 빅데이터에서도 효율적으로 학습 가능
최적점에서 탈출 가능
→ 경사 하강법이 지역 최적점(Local Minima)에 빠지는 것을 방지

❌ 단점
진동(Noise)이 심함
→ 최적점을 찾기 전에 값이 왔다 갔다 할 수 있음
수렴이 불안정할 수 있음
→ 너무 큰 학습률(Learning Rate)을 사용하면 발산할 수도 있음
최적의 가중치를 찾는 데 시간이 걸릴 수 있음
→ 일반 GD보다 조금 더 오래 걸릴 수도 있음

✅ 1) SGD 직접 구현 (NumPy 사용)
```python
import numpy as np

# 가상의 데이터
X = np.array([1, 2, 3, 4, 5])  # 입력값
y = np.array([2, 4, 6, 8, 10])  # 정답값 (y = 2x)

# 초기 가중치 설정
w = np.random.randn()
learning_rate = 0.01

# SGD 실행
for epoch in range(1000):  # 1000번 반복
    i = np.random.randint(0, len(X))  # 랜덤하게 데이터 선택
    gradient = 2 * (w * X[i] - y[i]) * X[i]  # 미분(gradient)
    w -= learning_rate * gradient  # 가중치 업데이트

print(f"학습된 가중치: {w}")  # 최적의 w는 약 2에 가까워야 함
```

랜덤으로 데이터를 하나씩 선택하여 가중치를 업데이트하는 방식 (SGD 적용)

✅2) Scikit-Learn에서 SGD 사용
```python
from sklearn.linear_model import SGDRegressor
import numpy as np

# 가상의 데이터 생성
X = np.array([[1], [2], [3], [4], [5]])  # 입력값
y = np.array([2, 4, 6, 8, 10])  # 출력값 (y = 2x)

# 모델 생성 및 학습
model = SGDRegressor(learning_rate="constant", eta0=0.01, max_iter=1000)
model.fit(X, y)

print(f"학습된 가중치: {model.coef_}")  # 출력: 약 [2.0]
```

 SGD의 확장된 버전 (Adam, RMSprop 등)
SGD는 기본적인 최적화 알고리즘이지만, 여러 가지 개선된 버전이 있어!

알고리즘	특징
SGD	기본적인 확률적 경사 하강법
SGD + Momentum	가속도를 추가하여 더 부드러운 업데이트
Adagrad	학습률을 자동으로 조정
RMSprop	Adagrad의 개선 버전, 학습률 조절
Adam	RMSprop + Momentum을 합친 강력한 최적화 방법


📌 Optimizer(최적화 알고리즘)란?
**Optimizer(최적화 알고리즘)**는 딥러닝이나 머신러닝 모델에서 손실 함수(Loss Function)를 최소화하기 위해 모델의 가중치(Weight)를 조정하는 알고리즘이야.

💡 쉽게 말하면, 모델이 점점 더 좋은 예측을 하도록 가중치를 조절하는 방법이 바로 **Optimizer(최적화 기법)**야!

1️⃣ Optimizer가 하는 일
모델이 처음에는 랜덤한 가중치(Weight)를 가짐.
데이터를 입력받아 예측을 수행하고, 정답과 비교하여 **손실(Loss)**을 계산.
손실을 줄이기 위해 가중치를 조금씩 수정.
이 과정을 반복하면서 모델이 점점 더 좋은 성능을 내도록 학습.
📌 즉, Optimizer는 가중치를 업데이트하는 역할!

2️⃣ Optimizer의 종류

✅ 1) SGD (Stochastic Gradient Descent, 확률적 경사 하강법)
가장 기본적인 최적화 알고리즘.
데이터를 랜덤하게 샘플링하여 경사 하강법(GD)을 수행.
학습이 빠르지만, 최적점에 도달하기 전에 많이 흔들릴 수 있음(Noise 문제).

```python

optimizer = torch.optim.SGD(model.parameters(), lr=0.01)
```

✅ 2) Momentum (모멘텀)
SGD의 단점(진동 문제)을 보완한 방법.
마치 공이 언덕을 내려가듯, 이전 업데이트 방향을 일정 부분 유지하면서 부드럽게 이동.
최적점 근처에서 진동을 줄이고 더 빠르게 수렴.
```python
optimizer = torch.optim.SGD(model.parameters(), lr=0.01, momentum=0.9)
```

✅ 3) RMSprop (Root Mean Square Propagation)
학습률(Learning Rate)을 자동으로 조정하는 방식.
변화가 심한 방향에서는 학습률을 작게 하고, 변화가 적은 방향에서는 학습률을 크게 조정.
순환 신경망(RNN)에서 많이 사용됨.

```python
optimizer = torch.optim.RMSprop(model.parameters(), lr=0.001)
```

✅ 4) Adam (Adaptive Moment Estimation)
SGD + Momentum + RMSprop의 장점을 결합한 최적화 기법.
현재 가장 많이 사용되는 Optimizer!
학습률을 자동 조절하고, 모멘텀을 적용하여 빠르게 최적점으로 이동.
딥러닝에서는 기본적으로 Adam을 많이 사용함.
```python
optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
```

✅ 5) Adagrad (Adaptive Gradient Algorithm)
학습률을 자동 조절하는 방법 중 하나.
하지만 학습이 진행될수록 학습률이 너무 작아져서 나중에 학습이 잘 안 될 수도 있음.
Adam이나 RMSprop보다 덜 사용됨.

✅ 6) AdamW (Adam Weight Decay)
Adam의 업그레이드 버전.
Adam은 과적합(Overfitting)이 발생할 수 있는 문제가 있는데, AdamW는 이를 방지해 줌.
최근 트렌드에서는 Adam 대신 AdamW를 더 많이 사용.

3️⃣ Optimizer 선택 기준
어떤 Optimizer를 써야 할까? 🤔

Optimizer	특징	추천 사용 사례
SGD	기본적인 경사 하강법	간단한 모델, 이론 학습
Momentum	SGD + 속도 유지	빠른 수렴이 필요한 경우
RMSprop	학습률 자동 조정	순환 신경망 (RNN, LSTM)
Adam	SGD + Momentum + RMSprop	대부분의 딥러닝 모델
AdamW	Adam 개선, 과적합 방지	최신 딥러닝 모델, NLP
💡 대부분의 경우 Adam 또는 AdamW를 사용하면 무난해!
💡 하지만 큰 데이터셋에서는 SGD + Momentum이 더 좋을 수도 있어!

4️⃣ Optimizer 사용 예제 (PyTorch & TensorFlow)

```python
import torch

# 모델 생성
model = torch.nn.Linear(10, 1)

# Optimizer 설정 (Adam 사용)
optimizer = torch.optim.Adam(model.parameters(), lr=0.001)

# 학습 루프
for epoch in range(100):
    optimizer.zero_grad()  # 이전 기울기 초기화
    output = model(torch.randn(10))  # 모델 예측
    loss = (output - torch.tensor(1.0)) ** 2  # 손실 계산
    loss.backward()  # 기울기 계산
    optimizer.step()  # 가중치 업데이트
```