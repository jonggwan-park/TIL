퍼셉트론(Perceptron)은 인공지능과 머신러닝의 기초가 되는 인공 신경망(Artificial Neural Network)의 가장 기본적인 형태예요. 1958년 프랭크 로젠블랫(Frank Rosenblatt)이 고안한 알고리즘으로, **인간 뉴런(Neuron)**을 모방한 수학적 모델이라고 할 수 있어요.

🧠 퍼셉트론 개념
퍼셉트론은 **입력(Input), 가중치(Weight), 활성화 함수(Activation Function), 출력(Output)**으로 구성됩니다.

입력(Input, 
𝑥
1
,
𝑥
2
,
.
.
.
𝑥
𝑛
x 
1
​
 ,x 
2
​
 ,...x 
n
​
 ): 퍼셉트론이 받아들이는 값 (예: 이미지의 픽셀값, 센서 데이터 등)
가중치(Weight, 
𝑤
1
,
𝑤
2
,
.
.
.
𝑤
𝑛
w 
1
​
 ,w 
2
​
 ,...w 
n
​
 ): 입력값이 얼마나 중요한지를 결정하는 값
가중합(Weighted Sum, 
𝑧
z): 입력값과 가중치를 곱한 값들의 합
𝑧
=
𝑤
1
𝑥
1
+
𝑤
2
𝑥
2
+
.
.
.
+
𝑤
𝑛
𝑥
𝑛
+
𝑏
z=w 
1
​
 x 
1
​
 +w 
2
​
 x 
2
​
 +...+w 
n
​
 x 
n
​
 +b
(여기서 
𝑏
b는 바이어스(Bias) 로, 모델의 유연성을 증가시키는 역할)
활성화 함수(Activation Function, 
𝑓
(
𝑧
)
f(z)): 출력값을 결정하는 함수 (ex. 계단 함수, 시그모이드 함수 등)
출력(Output, 
𝑦
y): 최종 결과값 (0 또는 1)


⚠ 퍼셉트론의 한계
퍼셉트론은 선형적으로 구분 가능한 문제(예: AND, OR 게이트)는 해결할 수 있지만, 선형적으로 구분할 수 없는 문제(예: XOR 게이트)는 해결할 수 없어요.
이 문제를 해결하기 위해 나온 것이 **다층 퍼셉트론(MLP, Multi-Layer Perceptron)**이며, 여기에 비선형 활성화 함수(ReLU, 시그모이드 등)를 추가하여 신경망을 발전시켰어요.

XOR 문제 해결을 위한 다층 퍼셉트론 (MLP) 구현

```python
import numpy as np
import tensorflow as tf
from tensorflow import keras

# 1️⃣ XOR 데이터셋 정의
X = np.array([[0, 0], [0, 1], [1, 0], [1, 1]])  # 입력 데이터
y = np.array([[0], [1], [1], [0]])  # XOR의 정답 (출력)

# 2️⃣ MLP 모델 생성
model = keras.Sequential([
    keras.layers.Dense(units=4, activation='relu', input_shape=(2,)),  # 은닉층 (뉴런 4개, ReLU 활성화 함수)
    keras.layers.Dense(units=1, activation='sigmoid')  # 출력층 (뉴런 1개, 시그모이드 활성화 함수)
])

# 3️⃣ 모델 컴파일 (손실 함수, 옵티마이저 설정)
model.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])

# 4️⃣ 모델 학습 (훈련)
model.fit(X, y, epochs=500, verbose=0)  # 500번 반복 학습 (verbose=0은 로그 출력 생략)

# 5️⃣ 학습된 모델을 사용해 예측 수행
predictions = model.predict(X)
predictions = (predictions > 0.5).astype(int)  # 0.5 이상이면 1, 아니면 0으로 변환

# 6️⃣ 결과 출력
for i, x in enumerate(X):
    print(f"입력 {x} -> 예측 출력 {predictions[i][0]}")
```

실행 결과 예시

입력 [0 0] -> 예측 출력 0
입력 [0 1] -> 예측 출력 1
입력 [1 0] -> 예측 출력 1
입력 [1 1] -> 예측 출력 0



📌 MLP 모델의 핵심 개념
은닉층 (Hidden Layer) 추가

퍼셉트론은 선형적으로 구분할 수 있는 문제만 해결 가능
XOR 문제는 비선형 문제이므로 은닉층을 추가해야 함
활성화 함수 사용

ReLU (Rectified Linear Unit): 은닉층에서 비선형성을 추가
Sigmoid: 출력층에서 0~1 사이 확률 값 출력
손실 함수와 옵티마이저

binary_crossentropy: 이진 분류 문제에 적합
adam: 학습률을 자동 조절하는 최적화 알고리즘