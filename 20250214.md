1. retriever (LangChain 추상화)
LangChain의 고수준 추상화

LangChain에서 제공하는 retriever 인터페이스는 다양한 벡터 스토어 또는 검색 엔진을 동일한 방법으로 사용할 수 있게 해 줍니다.
예: VectorStoreRetriever나 BM25Retriever, SelfQueryRetriever 등등…
추가적인 기능

LangChain의 retriever를 사용하면 메모리, 필터, re-ranking, chain 등 다른 LangChain 컴포넌트와 쉽게 연동할 수 있습니다.
예: retriever = vectorstore.as_retriever(search_kwargs={"k": 3}) 처럼 파라미터를 한 번에 주어, 내부적으로 chunking이나 필터링 로직을 적용하기도 쉽습니다.
유연성

LangChain 내부에서 다양한 RetrievalPipeline을 구현해, 파이프라인 모듈로 사용할 수 있습니다.
예: 질문을 요약 → 벡터 검색 → 필터링 등 복잡한 단계를 파이프라인 형태로 연결하기 용이합니다.

2. 벡터 스토어 직접 사용 (similarity_search)
낮은 수준(로우레벨) 접근

vector_db.similarity_search(query, k=1)처럼, 직접 벡터 스토어의 메서드를 호출합니다.
LangChain 외부에서도 동일한 벡터 DB(Chroma, Faiss 등)를 직접 쓰는 프로젝트에 적용할 수 있습니다.
직접 관리와 제어

검색 로직, 추가 기능(예: 메타데이터 필터링)을 직접 구현해야 합니다.
LangChain에서 제공하는 편의 기능 없이, 원하는 로직을 세밀하게 작성할 수 있는 장점이 있습니다.
상호 호환성

LangChain과 전혀 관계없이 쓸 수도 있고,
LangChain 일부 기능만 가져오면서, 나머지는 수동으로 관리할 수도 있어요.


vector db 연습
```python
import streamlit as st
import openai
import os
from dotenv import load_dotenv
import random
from langchain.vectorstores import Chroma
from langchain.embeddings.openai import OpenAIEmbeddings
from langchain.schema import Document

# .env 파일 로드
load_dotenv()
openai.api_key = os.getenv("OPENAI_API_KEY")

# OpenAI Embeddings 사용
embedding = OpenAIEmbeddings()

# 벡터 저장소 초기화
vector_db = Chroma(persist_directory="./chroma_db", embedding_function=embedding)

st.title("💬 RAG 기반 AI 면접관")
st.write("이 챗봇은 과거 대화를 학습하여 비슷한 맥락을 유지합니다.")

# 세션 상태 초기화 (대화 기록 저장용)
if "chat_history" not in st.session_state:
    st.session_state["chat_history"] = []

# 랜덤 면접 질문 리스트
interview_questions = [
    "객체 지향 프로그래밍(OOP)이란 무엇인가요?",
    "파이썬에서의 가비지 컬렉션(GC)은 어떻게 동작하나요?",
    "데이터베이스 정규화란 무엇인가요?",
    "RESTful API란 무엇인가요?",
    "TCP와 UDP의 차이점은 무엇인가요?",
]

# 면접관이 랜덤 질문 선택 (세션에 저장)
if "random_question" not in st.session_state:
    st.session_state["random_question"] = random.choice(interview_questions)

st.write(f"면접관: {st.session_state['random_question']}")

# 사용자 입력 받기
user_input = st.text_input("면접자의 답변을 입력하세요:", "")

if st.button("답변 제출"):
    if user_input:
        with st.spinner("면접관 피드백 생성 중..."):
            # 🔹 1. 과거 대화 중 유사한 내용 검색
            similar_conversations = vector_db.similarity_search(st.session_state["random_question"], k=1)

            if similar_conversations:
                retrieved_context = similar_conversations[0].page_content
            else:
                retrieved_context = "과거 대화 기록이 없습니다."

            # 🔹 2. GPT에 전달할 메시지 구성
            messages = [
                {"role": "system", "content": "당신은 면접관입니다. 면접자의 답변에 대해 면접관처럼 피드백을 제공하세요."},
                {"role": "user", "content": f"면접 질문: {st.session_state['random_question']}\n면접자의 답변: {user_input}\n\n참고할 과거 대화: {retrieved_context}"}
            ]

            # 🔹 3. GPT 응답 생성
            response = openai.ChatCompletion.create(
                model="gpt-3.5-turbo",
                messages=messages
            )
            answer = response["choices"][0]["message"]["content"]
            
            # 🔹 4. 대화 기록을 벡터 DB에 저장
            conversation_text = f"질문: {st.session_state['random_question']}\n답변: {user_input}\nGPT 응답: {answer}"
            vector_db.add_documents([Document(page_content=conversation_text)])

            # 🔹 5. 대화 기록을 세션에 저장
            st.session_state["chat_history"].append({"question": st.session_state['random_question'], "answer": answer})
            
            st.success(answer)

    else:
        st.warning("면접자의 답변을 입력하세요.")

# 새로운 질문 받기 버튼
if st.button("새로운 질문 받기"):
    st.session_state["random_question"] = random.choice(interview_questions)
    st.experimental_rerun()
```