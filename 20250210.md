🔍 super().__init__()
super().__init__()는 부모 클래스(상위 클래스)의 생성자를 호출하는 코드

즉, 자식 클래스(하위 클래스)가 부모 클래스의 기능을 그대로 가져오면서 추가적인 기능을 확장할 때 사용하는 것

쉽게 말해 부모의 능력을 물려받으면서 새로운 능력을 추가할 때 사용하는 코드

```python
# 부모 클래스 (Animal)
class Animal:
    def __init__(self, name):
        self.name = name
        print(f"{self.name}은(는) 동물입니다.")

# 자식 클래스 (Dog)
class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)  # 부모 클래스(Animal)의 __init__() 실행
        self.breed = breed
        print(f"{self.name}은(는) {self.breed}입니다.")

# 객체 생성
dog1 = Dog("바둑이", "골든 리트리버")
```

바둑이은(는) 동물입니다.
바둑이은(는) 골든 리트리버입니다.


super() 없이 하면 뭐가 문제인가?
만약 super()를 사용하지 않고, 부모 클래스의 기능을 직접 코딩한다면 코드가 길어지고, 중복이 발생

```python
class Animal:
    def __init__(self, name):
        self.name = name
        print(f"{self.name}은(는) 동물입니다.")

class Dog(Animal):
    def __init__(self, name, breed):
        # 부모 클래스 기능을 직접 구현 (중복 발생)
        self.name = name
        print(f"{self.name}은(는) 동물입니다.")
        
        self.breed = breed
        print(f"{self.name}은(는) {self.breed}입니다.")

dog2 = Dog("뽀삐", "시베리안 허스키")
```

뽀삐은(는) 동물입니다.
뽀삐은(는) 시베리안 허스키입니다.


부모 클래스(Animal)의 __init__() 기능을 중복해서 직접 써야 해.
만약 부모 클래스의 코드가 변경되면, 자식 클래스도 직접 수정해야 해서 유지보수가 어려워져.

🚀 super()의 핵심 정리
✅ super().__init__()의 역할

부모 클래스의 생성자 __init__()을 실행해서, 부모 클래스의 기능을 가져옴.
코드 중복을 줄이고, 유지보수를 쉽게 만들어 줌.
부모 클래스가 변경되더라도 자식 클래스에서 따로 수정할 필요가 없음.
✅ 언제 사용해야 할까?

부모 클래스의 기능을 유지하면서, 자식 클래스에 새로운 기능을 추가할 때 사용하면 돼.

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
        print(f"{self.name}, {self.age}세")

class Student(Person):
    def __init__(self, name, age, student_id):
        super().__init__(name, age)  # 부모 클래스(Person)의 __init__() 실행
        self.student_id = student_id
        print(f"학생 ID: {self.student_id}")

s1 = Student("홍길동", 16, "S12345")
```

홍길동, 16세
학생 ID: S12345
